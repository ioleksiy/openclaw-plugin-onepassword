# Copy to your Gateway process environment (do NOT commit real values).
# The 1Password service account token the plugin uses to authenticate.
# https://developer.1password.com/docs/service-accounts
OP_SERVICE_ACCOUNT_TOKEN=ops_replace_me
